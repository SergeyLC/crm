# Local environment overrides (copy to .env.local and customize)
# This file is for developer-specific settings and should not be committed

# Database (if you use a different local database)
# DATABASE_URL="postgresql://yourname:yourpassword@localhost:5432/loya_care_crm_local"

# Custom ports (if defaults are occupied)
# BACKEND_PORT=4002
# FRONTEND_PORT=3002

# Feature flags for local development
# USE_MOCK=true
# LOG_LEVEL=debug

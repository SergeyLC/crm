# =============================================================================
# LoyaCareCRM - Environment Configuration Template
# =============================================================================
# Скопируйте этот файл в .env и заполните значения для вашего окружения
# cp .env.example .env
# =============================================================================

# =============================================================================
# Общие настройки приложения
# =============================================================================
# IP адрес или домен сервера (без http://)
DOMAIN=217.154.173.36

# Окружение (production, staging, development)
NODE_ENV=production

# Версия приложения
NEXT_PUBLIC_APP_VERSION=production

# =============================================================================
# Frontend настройки (Next.js)
# =============================================================================
# Порт на котором запускается Frontend
FRONTEND_PORT=3000

# API URL для клиентских запросов (относительный путь через Nginx)
NEXT_PUBLIC_API_URL=/api

# Backend API URL (для server-side запросов)
NEXT_PUBLIC_BACKEND_API_URL=http://217.154.173.36/api

# =============================================================================
# Backend настройки (Express)
# =============================================================================
# Порт на котором запускается Backend
BACKEND_PORT=4000

# Уровень логирования (error, warn, info, debug)
LOG_LEVEL=info

# CORS origin (адрес фронтенда)
CORS_ORIGIN=http://217.154.173.36

# =============================================================================
# База данных PostgreSQL
# =============================================================================
# Имя базы данных
POSTGRES_DB=loyacrm

# Пользователь PostgreSQL
POSTGRES_USER=loyacrm

# Пароль PostgreSQL
# ВАЖНО: Сгенерируйте надежный пароль!
# Пример: openssl rand -base64 16
POSTGRES_PASSWORD=loyacrm2024secure

# =============================================================================
# Безопасность
# =============================================================================
# JWT Secret ключ для подписи токенов
# ВАЖНО: Сгенерируйте уникальный секрет!
# Пример: openssl rand -base64 32
JWT_SECRET=dGhpc2lzYXNlY3JldGp3dHNlY3JldGtleQ==

# =============================================================================
# Docker Images
# =============================================================================
# Frontend Docker образ
FRONTEND_IMAGE=loyacrm-frontend:latest

# Backend Docker образ
BACKEND_IMAGE=loyacrm-backend:latest

# =============================================================================
# Порты сервисов
# =============================================================================
# Nginx порт (внешний HTTP)
NGINX_PORT=80

# =============================================================================
# Дополнительные настройки (опционально)
# =============================================================================
# Для использования с Docker Registry (GitHub Container Registry)
# FRONTEND_IMAGE=ghcr.io/sergeylc/crm/frontend:latest
# BACKEND_IMAGE=ghcr.io/sergeylc/crm/backend:latest

# =============================================================================
# Генерация безопасных значений
# =============================================================================
# Используйте следующие команды для генерации безопасных значений:
#
# JWT Secret:
#   openssl rand -base64 32
#
# Database Password:
#   openssl rand -base64 16
#
# Пример полной генерации .env файла:
#   echo "JWT_SECRET=$(openssl rand -base64 32)" >> .env
#   echo "POSTGRES_PASSWORD=$(openssl rand -base64 16)" >> .env

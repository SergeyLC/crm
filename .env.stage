# =============================================================================
# LoyaCareCRM - STAGING Environment Configuration
# =============================================================================
# Конфигурация для staging окружения на сервере 217.154.173.36:8080
# =============================================================================

# =============================================================================
# Общие настройки приложения
# =============================================================================
# IP адрес или домен сервера (без http://)
DOMAIN=217.154.173.36

# Окружение (production, staging, development)
NODE_ENV=production

# Версия приложения (будет обновлена автоматически при деплое)
NEXT_PUBLIC_APP_VERSION=v1.0.0-staging

# =============================================================================
# Frontend настройки (Next.js)
# =============================================================================
# Порт на котором запускается Frontend внутри контейнера
FRONTEND_PORT=3000

# API URL для клиентских запросов (относительный путь через Nginx)
NEXT_PUBLIC_API_URL=/api

# Backend API URL (для server-side запросов) - STAGING порт 8080
NEXT_PUBLIC_BACKEND_API_URL=http://217.154.173.36:8080/api

# =============================================================================
# Backend настройки (Express)
# =============================================================================
# Порт на котором запускается Backend внутри контейнера
BACKEND_PORT=4000

# Уровень логирования (error, warn, info, debug)
LOG_LEVEL=info

# CORS Origin (разрешенные источники запросов)
CORS_ORIGIN=http://217.154.173.36:8080

# =============================================================================
# Database настройки (PostgreSQL)
# =============================================================================
# Имя базы данных для staging
POSTGRES_DB=loyacrm_staging

# Пользователь базы данных
POSTGRES_USER=loyacrm

# Пароль базы данных (ЗАМЕНИТЕ на надежный пароль!)
# Сгенерировать: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_STAGING_PASSWORD

# =============================================================================
# Security настройки
# =============================================================================
# JWT Secret для подписи токенов (ЗАМЕНИТЕ на уникальный секрет!)
# Сгенерировать: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_STAGING_JWT_SECRET

# =============================================================================
# Docker настройки
# =============================================================================
# Nginx внешний порт (STAGING использует 8080 вместо 80)
NGINX_PORT=8080

# Docker образы для staging
FRONTEND_IMAGE=loyacrm-frontend:staging
BACKEND_IMAGE=loyacrm-backend:staging

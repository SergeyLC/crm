# Backend Production Environment
# Non-sensitive configuration (can be committed)
# Secrets should be provided via environment variables or .env.production.local

# Server
PORT=4000
NODE_ENV=production

# Prisma
PRISMA_LOG_LEVEL=warn

# JWT
JWT_EXPIRES_IN="1h"

# Features
USE_MOCK=false

# Logging
LOG_LEVEL=info

# === SENSITIVE - Set via environment variables ===
# DATABASE_URL="postgresql://user:password@host:5432/db"
# JWT_SECRET="min-32-char-random-string"
# CORS_ORIGIN="https://your-domain.com"

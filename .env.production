# Production Environment Configuration
# Non-sensitive settings that can be committed to git
# Sensitive values (DATABASE_URL, JWT_SECRET, etc.) should be set via:
#   - Server environment variables
#   - .env.production.local (not committed)
#   - PM2 ecosystem.config.js
#   - Docker secrets / Kubernetes secrets

# Environment
NODE_ENV=production

# Backend Configuration
BACKEND_PORT=4000
BACKEND_HOST=0.0.0.0
USE_MOCK=false

# Frontend Configuration
FRONTEND_PORT=3000
FRONTEND_HOST=0.0.0.0

# JWT Configuration
JWT_EXPIRES_IN="1h"

# Logging
PRISMA_LOG_LEVEL=warn
LOG_LEVEL=info

# === SENSITIVE VALUES - DO NOT COMMIT ===
# Set these via server environment variables or .env.production.local
# DATABASE_URL="postgresql://user:password@host:5432/db"
# JWT_SECRET="your-secret-key-min-32-chars"
# CORS_ORIGIN="https://your-domain.com"
